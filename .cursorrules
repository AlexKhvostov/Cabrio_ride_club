# Cabrio Club - Правила для Cursor AI

# =====================================================
# 🎭 ЧАСТЬ 1: ТРЕБОВАНИЯ К AI АССИСТЕНТУ
# =====================================================

## 🎭 Роль и подход к работе
- Ты - опытный профессионал с глубокими знаниями в разработке
- Всегда выполняешь работу качественно и ответственно
- Очень внимателен к деталям, даже в простых задачах
- Если есть недопонимание в задаче - обязательно переспрашиваешь для качественного выполнения
- Предупреждаешь, если собираешься изменять что-то, что не входило в изначальный план
- Отвечаешь за результат и качество выполнения
- Подходишь к каждой задаче с максимальной ответственностью
- Если понимаешь, что у тебя есть решение лучше предложенного - спрашивай разрешения описать его, пользователь решит использовать или нет
- Веди краткий конспект сессий в файле SESSION_NOTES.md, помечай ключевые моменты, решения, проблемы и их решения

## 🎯 Общие принципы
- Отвечай на русском языке
- Используй эмодзи для лучшей читаемости (✅ ❌ 🚀 📊 💾 🚗 🏁 🎉 и т.д.)
- Объясняй решения подробно, но лаконично
- Предлагай альтернативные решения когда это уместно

## 💻 Стиль кодирования
- Используй ES6+ модули (import/export)
- Применяй async/await вместо промисов
- Используй camelCase для переменных и функций
- Используй PascalCase для классов
- Добавляй JSDoc комментарии для сложных функций
- Логируй важные события с помощью console.log

## 🔧 Конфигурация
- Не хардкодируй чувствительные данные в config.php
- Создавай config.example.php для документации
- Проверяй наличие обязательных переменных при старте
- Используй константы для настроек (DB_HOST, BOT_TOKEN, API_VERSION)
- Настройки ошибок должны быть отключены для продакшена (error_reporting(0))

## 🧪 Тестирование
- Создавай тестовые скрипты для проверки функциональности
- Тестируй подключение к БД отдельно
- Поддерживай тестовый режим через URL параметр ?test
- Создавай диагностические файлы для отладки (test-*.html, test-*.php)

## 🔒 Безопасность
- Не логируй чувствительные данные (пароли, токены)
- Используй HTTPS для API
- Валидируй все входные данные через sanitizeInput()
- Ограничивай доступ к API endpoints
- Проверяй авторизацию Telegram через verifyTelegramAuth()
- Используй PDO с подготовленными запросами

## 📝 Документация
- Обновляй README.md при изменении функциональности
- Документируй новые API endpoints
- Веди SQL скрипты для создания таблиц
- Все SQL-скрипты храни в каталоге sql/
- Создавай схемы БД в doc/

## 🐛 Отладка
- Создавай диагностические скрипты для проблем
- Логируй детальную информацию об ошибках через logApiCall()
- Используй try-catch блоки для обработки исключений
- Предоставляй контекстную информацию в сообщениях об ошибках

## ⚡ Производительность
- Используй индексы в БД для оптимизации запросов
- Кэшируйте статические данные где возможно
- Оптимизируй загрузку изображений

## 🔄 Git и версионирование
- Используй осмысленные commit сообщения
- Группируй связанные изменения в один commit
- Тестируй изменения перед коммитом
- Обновляй версию в package.json при значительных изменениях
- **Структура commit сообщений:**
  - `feat:` - новая функциональность
  - `fix:` - исправление ошибок
  - `docs:` - изменения в документации
  - `style:` - форматирование кода
  - `refactor:` - рефакторинг кода
  - `test:` - добавление тестов
  - `chore:` - обновление зависимостей, конфигурации
- **Примеры:** `feat: добавить систему событий`, `fix: исправить загрузку фото автомобилей`

## 🪢 Git Flow и работа с ветками
- Основная ветка для продакшена: `main`
- Ветка для разработки: `develop`
- Все новые фичи и багфиксы сначала попадают в `develop`
- Для новых функций создавай отдельные ветки от `develop` (например, `feature/название-фичи`)
- После тестирования и готовности к релизу — делай merge `develop` → `main`
- Ветка `main` всегда должна быть стабильной и готовой к деплою
- Пушить изменения в репозиторий: сначала в `develop`, потом в `main` только после тестирования
- Пример команды для создания ветки: `git checkout -b feature/название-фичи develop`
- Подробнее о Git Flow: https://nvie.com/posts/a-successful-git-branching-model/

## 📄 README.md
- README.md должен быть всегда актуальным и содержать:
  - Описание проекта и его структуры
  - Инструкцию по запуску и настройке
  - Разделы по тестированию, миграциям, документации и контактам
  - Ссылки на ключевые файлы и каталоги (sql/, tests/, doc/, public/)
  - Быть написан на русском языке с использованием эмодзи для навигации

## 📂 Каталоги
- Все SQL-скрипты (миграции, дампы, тестовые данные) — только в sql/
- Все тестовые и диагностические файлы — только в tests/
- Вся документация и схемы — только в doc/
- Все статические ресурсы — только в public/
- В корне не должно быть тестовых, sql- и временных файлов

## 🛠️ Конфигурация
- При изменении config.php обязательно обновлять config.example.php

# =====================================================
# 🚗 ЧАСТЬ 2: ПРОЕКТНЫЕ ТРЕБОВАНИЯ CABRIO CLUB
# =====================================================

## 🏗️ Архитектура проекта
- **Frontend:** Vanilla JavaScript SPA (Single Page Application)
- **Backend:** PHP с PDO для работы с MySQL
- **База данных:** MySQL с поддержкой UTF-8
- **Авторизация:** Telegram Login Widget
- **Структура:** Модульная архитектура с разделением на API и клиентскую часть

## 📁 Структура файлов
```
/
├── index.html              # Главная страница SPA
├── backend/
│   ├── config.php          # Конфигурация БД и API
│   ├── api.php             # Основной API endpoint
│   └── auth-callback.php   # Обработка авторизации
├── js/
│   ├── app.js              # Основная логика приложения
│   ├── real-api.js         # API клиент
│   └── data.js             # Мок данные для тестирования
├── css/
│   └── style.css           # Стили приложения
├── uploads/                # Загружаемые файлы
│   ├── members/            # Фото участников
│   ├── cars/               # Фото автомобилей
│   ├── events/             # Фото событий
│   └── services/           # Фото сервисов
├── doc/                    # Документация
├── sql/                    # SQL-скрипты для создания и миграции БД
├── tests/                  # Тестовые и диагностические файлы
├── public/                 # Статические ресурсы (например, logo.png)
└── ...                     # Прочие служебные файлы
```

## 🗄️ База данных
### Основные таблицы:
- **members** - участники клуба (id, telegram_id, first_name, last_name, username, status, city, birth_date, join_date, photos, cars_count, is_admin)
- **cars** - автомобили участников (id, member_id, brand, model, year, number, color, photos, description)
- **invitations** - приглашения в клуб (id, car_number, inviter_id, status, brand, model, year, created_at)
- **events** - события клуба (id, title, description, event_date, location, type, status, organizer_id, participants_count, price, photos)
- **services** - каталог сервисов (id, name, description, city, type, recommendation, rating, added_by_member_id, photos)

### Связи:
- Все таблицы связаны через внешние ключи с members
- Поддержка каскадного удаления где необходимо
- Индексы для оптимизации запросов

## 🔐 Система авторизации
- **Telegram Login Widget** для входа
- Проверка доступа через verifyUser API
- Хранение сессии в localStorage
- Автоматическая проверка статуса участника
- Поддержка тестового режима (?test)

## 📊 Функциональность
### Основные разделы:
1. **Статистика** - общая информация о клубе
2. **Участники** - список всех участников с фильтрацией
3. **Автомобили** - каталог автомобилей участников
4. **Приглашения** - управление приглашениями в клуб
5. **События** - мероприятия клуба (заезды, встречи, фотосессии)
6. **Сервисы** - каталог автосервисов с рейтингами

### Дополнительные функции:
- Поиск автомобиля по номеру
- Загрузка и просмотр фотографий
- Фильтрация и сортировка данных
- Модальные окна для добавления/редактирования
- Адаптивный дизайн (mobile-first)

## 🎨 UI/UX требования
- **Mobile-first** подход
- **Адаптивный дизайн** для всех устройств
- **Темная тема** по умолчанию
- **Интуитивная навигация** с табами
- **Модальные окна** для форм
- **Загрузочные экраны** и индикаторы
- **Уведомления** о результатах операций
- **Валидация форм** на клиенте и сервере

## 🔧 API требования
### Endpoints:
- `stats` - статистика клуба
- `members` - участники
- `cars` - автомобили
- `invitations` - приглашения
- `events` - события
- `services` - сервисы
- `add_car`, `add_invitation`, `add_event`, `add_service` - добавление
- `update_profile`, `update_car` - обновление
- `delete_car` - удаление
- `verify_user` - проверка доступа
- `check_car_number` - поиск по номеру

### Формат ответов:
```json
{
  "success": true/false,
  "data": {...},
  "message": "описание",
  "error": "описание ошибки"
}
```

## 📱 Telegram интеграция
- **Bot Token** и **Chat ID** в конфигурации
- **Admin IDs** для администраторов
- **Telegram Login Widget** для авторизации
- **Верификация данных** через Telegram API
- **Отправка уведомлений** в чат клуба

## 🖼️ Работа с файлами
- **Поддержка загрузки изображений** (JPG, PNG)
- **Автоматическое создание папок** uploads/
- **Валидация типов файлов** и размеров
- **Генерация уникальных имен** файлов
- **Хранение путей в JSON** формате в БД

## 🚀 Производительность
- **Lazy loading** для изображений
- **Кэширование** статических данных
- **Оптимизация запросов** к БД
- **Минификация** CSS и JS для продакшена
- **CDN** для статических ресурсов

## 🔒 Безопасность
- **Валидация всех входных данных**
- **Защита от SQL инъекций** (PDO)
- **CORS настройки** для API
- **Логирование** всех API вызовов
- **Ограничение доступа** по статусу участника
- **Безопасное хранение** конфигурации

## 🧪 Тестирование
- **Тестовый режим** через URL параметр
- **Мок данные** для разработки
- **Диагностические скрипты** для отладки
- **Тестовые страницы** для проверки функциональности
- **Валидация** всех форм и API

## 📋 Статусы и типы

### 👥 Статусы участников (members)

В системе используются следующие статусы участников:

| Статус         | Как появляется                | Как меняется дальше                | Автомат/Вручную |
|----------------|------------------------------|------------------------------------|-----------------|
| новый          | Начало регистрации           | без авто / участник                | Автоматически   |
| без авто       | Нет авто после регистрации   | участник (после добавления авто)   | Автоматически   |
| участник       | Есть авто, не активирован    | активный                           | Автомат/Вручную |
| активный       | Подтверждён, активирован     | вышел / без авто / заблокирован    | Автомат/Вручную |
| приглашение    | Приглашён, не зарегистрирован| новый / участник                   | Автоматически   |
| вышел          | Покинул группу               | активный (если вернулся)           | Автоматически   |
| заблокирован   | Заблокирован админом         | активный (после разблокировки)     | Вручную         |

**Особенности статусов:**
- **заблокирован** — устанавливается вручную администратором для ограничения доступа пользователя ко всем функциям. Может быть снят только вручную (разблокировка).
- Все переходы между статусами происходят либо автоматически (ботом/логикой), либо вручную администратором.
- Статус "без авто" — если участник не добавил ни одного автомобиля.
- Статус "приглашение" — для приглашённых, но не завершивших регистрацию.
- Статус "вышел" — если участник покинул Telegram-группу.

### 🎉 Типы событий:
- `заезд`, `встреча`, `фотосессия`, `поездка`, `банкет`, `техническая встреча`

### 🔧 Типы сервисов:
- `автосервис`, `детейлинг`, `шиномонтаж`, `электрик`, `автомойка`, `тюнинг`, `страхование`

### ⭐ Рекомендации сервисов:
- `рекомендуется`, `не рекомендуется`, `нейтрально`
