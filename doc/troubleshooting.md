# 🚨 Устранение ошибки "Internal Server Error"

## 🎯 Проблема
При обращении к скриптам получаете ошибку:
```
Internal Server Error
The server encountered an internal error or misconfiguration and was unable to complete your request.
```

## 🔍 Пошаговая диагностика

### Шаг 1: Простая проверка PHP
Создайте файл `backend/test.php`:
```php
<?php
echo "PHP работает!";
phpinfo();
?>
```

Если этот файл не работает - проблема с PHP на сервере.

### Шаг 2: Проверка подключения к БД
Откройте: `http://ваш-домен.ru/backend/test-connection.php`

Этот скрипт покажет:
- ✅ Подключается ли config.php
- ✅ Определены ли константы БД
- ✅ Работает ли подключение к MySQL
- ✅ Какие таблицы существуют

### Шаг 3: Проверка логов ошибок
Посмотрите логи ошибок сервера:
- **cPanel**: Файлы → Журналы ошибок
- **Прямой доступ**: `/var/log/apache2/error.log` или `/var/log/nginx/error.log`

### Шаг 4: Включение отображения ошибок PHP
Добавьте в начало проблемного файла:
```php
<?php
error_reporting(E_ALL);
ini_set('display_errors', 1);
?>
```

## 🔧 Частые причины ошибок

### 1. Синтаксические ошибки PHP
**Симптомы**: Internal Server Error при обращении к .php файлам
**Решение**: Проверить синтаксис через `php -l filename.php`

### 2. Проблемы с кодировкой
**Симптомы**: Ошибки при работе с русским текстом
**Решение**: 
- Сохранить файлы в UTF-8 без BOM
- Проверить настройки БД: `utf8mb4_unicode_ci`

### 3. Отсутствие прав доступа
**Симптомы**: Permission denied
**Решение**: Установить права 644 для .php файлов, 755 для папок

### 4. Проблемы с путями файлов
**Симптомы**: require_once не находит файлы
**Решение**: Использовать абсолютные пути или правильные относительные

### 5. Превышение лимитов PHP
**Симптомы**: Скрипт обрывается без ошибок
**Решение**: Увеличить `memory_limit`, `max_execution_time`

## 🛠 Исправленные версии скриптов

### Используйте упрощенные версии:
1. **`backend/test-connection.php`** - базовая диагностика
2. **`backend/simple-populate.php`** - упрощенное заполнение БД
3. **`backend/check-database.php`** - исправленная версия проверки БД

## 📋 Чек-лист диагностики

- [ ] PHP работает (test.php показывает phpinfo)
- [ ] config.php подключается без ошибок
- [ ] Константы БД определены правильно
- [ ] Подключение к MySQL работает
- [ ] Таблицы созданы в БД
- [ ] Права доступа к файлам корректные
- [ ] Логи ошибок проверены

## 🚀 Последовательность действий

### 1. Базовая диагностика:
```
http://ваш-домен.ru/backend/test.php
http://ваш-домен.ru/backend/test-connection.php
```

### 2. Если подключение к БД работает:
```
http://ваш-домен.ru/backend/simple-populate.php
```

### 3. Если все работает:
```
http://ваш-домен.ru/backend/api.php?action=stats
http://ваш-домен.ru/index.html
```

## 🔍 Возможные ошибки и решения

### ❌ "Call to undefined function getDbConnection()"
**Причина**: config.php не подключен или содержит ошибки
**Решение**: Проверить `require_once 'config.php';`

### ❌ "Access denied for user"
**Причина**: Неправильные реквизиты БД
**Решение**: Проверить DB_HOST, DB_USER, DB_PASSWORD в config.php

### ❌ "Table doesn't exist"
**Причина**: Таблицы не созданы в БД
**Решение**: Выполнить SQL скрипт `doc/database-schema.sql`

### ❌ "Syntax error, unexpected..."
**Причина**: Ошибка в PHP коде
**Решение**: Проверить синтаксис, кавычки, скобки

### ❌ "Cannot modify header information"
**Причина**: Вывод до header() функций
**Решение**: Убрать пробелы/текст перед <?php

## 💡 Рекомендации

1. **Всегда начинайте с простых скриптов** (test.php, test-connection.php)
2. **Проверяйте логи ошибок** сервера
3. **Включайте отображение ошибок** во время разработки
4. **Делайте резервные копии** перед изменениями
5. **Тестируйте по частям** - не запускайте сразу сложные скрипты

## 📞 Если ничего не помогает

1. Свяжитесь с технической поддержкой хостинга
2. Проверьте версию PHP (должна быть 7.4+)
3. Убедитесь что MySQL доступен
4. Проверьте квоты дискового пространства 